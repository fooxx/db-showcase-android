<layout>

	<data>

		<variable
			name="viewModel"
			type="cz.koto.misak.dbshowcase.android.mobile.viewModel.MainActivityViewModel"/>

		<import type="cz.kinst.jakub.view.StatefulLayout.State" />
	</data>

	<android.support.design.widget.CoordinatorLayout
		xmlns:android="http://schemas.android.com/apk/res/android"
		xmlns:app="http://schemas.android.com/apk/res-auto"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:fitsSystemWindows="true">

		<android.support.v7.widget.Toolbar
			android:id="@+id/toolbar"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:background="@color/global_color_primary"
			android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
			app:title="@string/app_title"/>

		<cz.kinst.jakub.view.StatefulLayout
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:layout_marginTop="?attr/actionBarSize"
			app:emptyLayout="@layout/default_placeholder_empty"
			app:progressLayout="@layout/default_placeholder_progress"
			app:offlineLayout="@layout/default_placeholder_offline"
			app:state="@{viewModel.state}">

			<android.support.v7.widget.RecyclerView
				android:id="@+id/recycler"
				android:layout_width="match_parent"
				android:layout_height="match_parent"
				android:background="@android:color/holo_blue_light"
				app:adapter="@{viewModel.getAdapter()}"/>

		</cz.kinst.jakub.view.StatefulLayout>
	</android.support.design.widget.CoordinatorLayout>
</layout>