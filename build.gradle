apply from: "version.app.gradle"
apply from: "version.libs.gradle"

buildscript {
	repositories {
		jcenter()
	}
	dependencies {
		classpath 'com.android.tools.build:gradle:2.1.0'
		classpath 'com.neenbedankt.gradle.plugins:android-apt:1.8'
        classpath "io.realm:realm-gradle-plugin:0.90.1"//REALM.IO
	}
}

allprojects {
	repositories {
		jcenter()
		maven { url "https://jitpack.io" } //DBFLOW
	}
}